#include <stdio.h>
#include <stdlib.h>
int main()
{
    int n,s=0;float avg;
    scanf("%d", &n);
    int a[n],i;
    for(i=0;i<n;i++)
        {
            scanf("%d", &a[i]);
            s=s+a[i];
        }
    avg=(float)s/n;
    printf("%0.2f", avg);
}
